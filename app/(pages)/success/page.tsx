import React from 'react';
import { Navbar, Footer } from '@/components';

const Success = () => {
    return (
        <div>
            <Navbar />
            Success
            <Footer />
        </div>
    );
};

export default Success;
