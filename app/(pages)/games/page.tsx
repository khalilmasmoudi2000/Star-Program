import React from 'react';
import { Navbar, Footer } from '@/components';

const Games = () => {
    return (
        <div>
            <Navbar />
            Games
            <Footer />
        </div>
    );
};

export default Games;
