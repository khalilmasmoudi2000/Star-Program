import React from "react";
import styles from '@/app/index';
import Link from 'next/link';
import Image from 'next/image';

const Navbar = () => {
    return (
        <div className="bg-secondary-white overflow-hidden h-screen w-screen">
            Navbar
        </div>
    );
};

export default Navbar;
